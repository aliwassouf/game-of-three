spring:
  main:
    allow-bean-definition-overriding: true
  rabbitmq:
    host: localhost
    port: 5672
    username: rabbitmq
    password: rabbitmq
  cloud:
    stream:
      bindings:
        test:
          destination: test

server:
  port: 8082


messaging:
  current:
    queue: serviceb
    exchange: game
    routingkey: serviceb
  target:
    exchange: game
    routingKey: servicea